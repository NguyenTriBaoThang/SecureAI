# Đặt 4 file model vào đây (download từ Google Colab):
#
#   secureai_bilstm_attention.pt   ← weights của BiLSTM+Attention
#   tokenizer.pkl                  ← character tokenizer
#   label_encoder.pkl              ← sklearn LabelEncoder
#   model_metadata.json            ← metrics và config
